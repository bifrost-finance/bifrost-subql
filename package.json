{
  "name": "bifrost-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build --mode=dev",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Bifrost",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^12.3.1",
    "@subql/types": "3.11.1",
    "typescript": "^4.4.4",
    "@subql/cli": "5.1.1"
  },
  "dependencies": {
    "bignumber.js": "^9.0.1"
  }
}
