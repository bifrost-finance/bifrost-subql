{
  "name": "bifrost-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Bifrost",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^3",
    "@subql/cli": "^0.7.0",
    "@subql/types": "^0.6.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@bifrost-finance/types": "^1.0.93"
  }
}
