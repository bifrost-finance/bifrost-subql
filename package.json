{
  "name": "bifrost-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build --mode=dev",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Bifrost",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/types": "latest",
    "typescript": "^4.4.4",
    "@subql/cli": "latest"
  },
  "dependencies": {
    "@bifrost-finance/api": "1.3.14",
    "bignumber.js": "^9.0.1"
  }
}
